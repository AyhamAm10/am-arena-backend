/auth/register:
  post:
    tags:
      - Auth
    summary: Register a new user
    description: >
      Creates a user account. Request body is JSON validated by authRegisterSchema
      (full_name, gamer_name, email, password required; phone and profile_picture_url optional).
      Sets refreshToken in an HTTP-only cookie. The controller supports req.file for a profile image
      only if a multipart upload middleware is attached to the route (current route uses JSON only).
    operationId: register
    security: []
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AuthRegisterBody'
    responses:
      '201':
        description: User created successfully
        content:
          application/json:
            schema:
              allOf:
                - $ref: '#/components/schemas/ApiResponse'
                - type: object
                  properties:
                    data:
                      $ref: '#/components/schemas/AuthTokensResponse'
      '400':
        description: Validation error

/auth/login:
  post:
    tags:
      - Auth
    summary: Login
    description: >
      Returns user (without password_hash) and accessToken in JSON.
      Sets refreshToken in an HTTP-only cookie (same cookie options as register).
    operationId: login
    security: []
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AuthLoginBody'
    responses:
      '200':
        description: Logged in successfully
        content:
          application/json:
            schema:
              allOf:
                - $ref: '#/components/schemas/ApiResponse'
                - type: object
                  properties:
                    data:
                      $ref: '#/components/schemas/AuthTokensResponse'
      '400':
        description: Invalid credentials

/auth/logout:
  post:
    tags:
      - Auth
    summary: Logout
    description: Clears refresh token cookie. Requires authentication.
    operationId: logout
    security:
      - bearerAuth: []
    responses:
      '200':
        description: Logged out successfully
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApiResponse'
      '401':
        description: Unauthorized

/auth/current-user:
  get:
    tags:
      - Auth
    summary: Get current user
    description: Returns the authenticated user profile.
    operationId: getMe
    security:
      - bearerAuth: []
    parameters:
      - name: Accept-Language
        in: header
        schema:
          type: string
          example: en
    responses:
      '200':
        description: Current user data
        content:
          application/json:
            schema:
              allOf:
                - $ref: '#/components/schemas/ApiResponse'
                - type: object
                  properties:
                    data:
                      type: object
                      description: User object (without password)
      '401':
        description: Unauthorized
      '404':
        description: User not found
